#include <stdio.h>

int main()
{
int n, rem, c=0;

printf("Enter the number\\n");
scanf("%d", &n);

while(n!=0)
{
    rem=n%10;
    n=n/10;
    ++c;
}
printf("Number of digits are %d", c);
return 0;
}
